import React from "react";
import ReactDOM from "react-dom/client";
import NovaPlayer from "./NovaPlayer";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NovaPlayer />
  </React.StrictMode>
);
